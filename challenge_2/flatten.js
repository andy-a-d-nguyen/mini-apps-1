// a function which takes in a nested obj
const flatten = (obj) => {
  // get keys of 1st level obj as an array

  // create a var to store csv formatted data

  // iterate over keys
    // if current key's value is empty array
      // return
    // if current key is not an array
      // add key's value to result
    // if current key's value is not an empty array
      // recursively call function
    // add newline to result
}